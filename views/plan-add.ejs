<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Thêm Plan</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100">
  <nav class="bg-blue-600 text-white p-4">
    <div class="container mx-auto flex justify-between">
      <h1 class="text-2xl font-bold">Admin Panel</h1>
      <a href="/logout" class="bg-red-500 px-4 py-2 rounded hover:bg-red-600">Đăng xuất</a>
    </div>
  </nav>
  <div class="container mx-auto p-4">
    <h2 class="text-xl font-bold mb-4">Thêm Plan</h2>
    <% if (error) { %>
      <p class="text-red-500 mb-4"><%= error %></p>
    <% } %>
    <form action="/admin/plans/add" method="POST" class="bg-white p-4 rounded shadow">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <div>
          <label class="block text-gray-700 mb-2">Plan Name</label>
          <input type="text" name="plan" class="w-full p-2 border rounded" required>
        </div>
        <div>
          <label class="block text-gray-700 mb-2">Price</label>
          <input type="number" name="price" class="w-full p-2 border rounded" required>
        </div>
        <div>
          <label class="block text-gray-700 mb-2">Description</label>
          <input type="text" name="description" class="w-full p-2 border rounded" required>
        </div>
      </div>
      <div class="mt-4">
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Thêm</button>
        <a href="/admin?tab=plans" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 ml-2">Hủy</a>
      </div>
    </form>
  </div>
</body>
</html>